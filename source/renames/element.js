export const query = (element, selector) => element.querySelector(selector);

export const queryAll = (element, selector) => element.querySelectorAll(selector);

export const addListener = (element, listener) => element.addEventListenever(listener);

export const removeListener = (element, listener) => element.removeEventListenever(listener);