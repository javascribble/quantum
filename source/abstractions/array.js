const array = Array;
export const isArray = array.isArray;

export const map = (array, delegate) => array.map(delegate);
export const filter = (array, delegate) => array.filter(delegate);
export const includes = (array, value) => array.includes(value);
export const forEach = (array, delegate) => array.forEach(delegate);